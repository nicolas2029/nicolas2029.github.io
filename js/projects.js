class Project {
  constructor(name, descripcion, imgs) {
    this.name = name;
    this.descripcion = descripcion;
    this.imgs = imgs;
  }
}

const Projects = new Map({
  1: { name: "" },
});

const addCaptures = (project_id) => {};
